host=`hostname`
if [[ "$host" == "Barteks-Air" ]]; then
    source ~/dotfiles/zsh/zpreztorc
    source ~/.zprezto/init.zsh
    source ~/.zprezto/runcoms/zshrc
    export CLICOLOR=YES
else
    source /etc/zsh/zpreztorc
    source /usr/lib/prezto/init.zsh
    source /usr/lib/prezto/runcoms/zshrc
    export TERM=xterm-termite
    eval `dircolors ~/.dircolors`
fi


export EDITOR=vim
export TASKRC=~/drive/taskwarrior/work/taskrc
export TASKDATA=~/drive/taskwarrior/work/task
export PATH=/home/bata/.gem/ruby/2.1.0/bin:$PATH
export PATH=/home/bata/dotfiles/scripts:$PATH

if [[ $TERM == xterm-termite ]]; then
  . /etc/profile.d/vte.sh
    __vte_osc7
fi
if [[ "$host" == "Barteks-Air" ]]; then
	export PAGER=/usr/local/bin/vimpager
else
	export PAGER=/usr/bin/vimpager
fi
alias less=$PAGER
alias zless=$PAGER

unset GREP_COLOR
unset GREP_OPTIONS

alias gst='git status'
alias gup='git pull --rebase'
alias builds='cd ~/.builds'
source ~/dotfiles/zsh/k.sh

export GOPATH=~/go
export PATH=$PATH:$GOPATH/bin
