#!/bin/bash


delta_command=/opt/homebrew/Cellar/git-delta/0.18.2_3/bin/delta

if [[ ! ($(darkMode) =~ 'Dark') ]]; then
	# shellcheck disable=2068
	$delta_command --light $@
else
	# shellcheck disable=2068
	$delta_command --dark $@
fi
