[user]
	name = Bartosz Taczała
	email = bartosz.taczala@mobica.com

[merge]
	tool = vimdiff
[gui]
	editor = gvim
	historybrowser = gitk
[diff]
	tool = vimdiff
[color]
	sh = auto
	ui = auto
[core]
	editor = nvim
	autocrlf = input
	excludesfile = ~/.gitignore
[alias]
	lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
	new = !sh -c 'git log $1@{1}..$1@{0} "$@"'
	ll = log --pretty=format:"%C(yellow)%h%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate --numstat
	la = "!git config -l | grep alias | cut -c 7-"
	ld = log --pretty=format:"%C(yellow)%h\\ %ad%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate --date=relative
	fl = log -u
    pub = "!f() { git push -u ${1:-origin} HEAD:`git config branch.$(git name-rev --name-only HEAD).merge | sed -e 's@refs/heads/@refs/for/@'`; }; f"
    showtool = "!f() { git difftool $1^ $1; }; f"
    added = difftool --cached
[difftool]
[push]
	default = simple
[gitreview]
	username = bataczal
	remote = origin
[cola]
	fontdiff = Hack,9,-1,5,50,0,0,0,0,0
	spellcheck = false
[difftool]
	prompt = false
	tool = vimdiff
[difftool "icdiff"]
    cmd = /usr/bin/icdiff --line-numbers $LOCAL $REMOTE
[pager]
    difftool = true
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
[review "gerrit.it.here.com"]
	username = bataczal
[url "ssh://bataczal@rep-gerrit-ams.it.here.com"]
	insteadOf = ssh://bataczal@gerrit.it.here.com
[url "ssh://bataczal@gerrit.it.here.com"]
	pushInsteadOf = ssh://bataczal@gerrit.it.here.com
